const defaultState = {
  byId: {},
  allIds: []
}
// no actions to update redux store for now ): 
const rewardReducer = (state = defaultState, action) => {
  return state;
}

export default rewardReducer